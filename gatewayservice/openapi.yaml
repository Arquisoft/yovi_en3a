openapi: 3.0.0
info:
  title: YOVI API
  version: 1.0.0
  description: API gateway that routes requests to the corresponding microservices

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

paths:
  /api/users/register:
    post:
      summary: Register a new user
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [username, email, password]
              properties:
                username:
                  type: string
                  example: user1
                email:
                  type: string
                  example: user1@test.com
                password:
                  type: string
                  example: User1234
                age:
                  type: integer
                  example: 25
                country:
                  type: string
                  example: Spain
      responses:
        '201':
          description: User created
        '400':
          description: Validation error
        '409':
          description: Username or email already in use

  /api/users/login:
    post:
      summary: Login
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [username, password]
              properties:
                username:
                  type: string
                  example: user1
                password:
                  type: string
                  example: User1234
      responses:
        '200':
          description: Login successful
          content:
            application/json:
              schema:
                type: object
                properties:
                  token:
                    type: string
                  userId:
                    type: string
        '400':
          description: Validation error
        '401':
          description: Wrong credentials

  /api/users/stats/{userId}:
    get:
      summary: Get personal stats
      security:
        - bearerAuth: []
      parameters:
        - name: userId
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: User stats
        '400':
          description: Invalid userId
        '404':
          description: Stats not found

  /api/users/stats/ranking:
    get:
      summary: Get global ranking
      security:
        - bearerAuth: []
      parameters:
        - name: sortBy
          in: query
          required: false
          schema:
            type: string
            enum: [wins, winRate]
            default: wins
      responses:
        '200':
          description: Ranking
        '400':
          description: Invalid sort method

  /api/game-manager/create/{gameName}:
    post:
      summary: Create a new game
      security:
        - bearerAuth: []
      parameters:
        - name: gameName
          in: path
          required: true
          schema:
            type: string
            enum: [standard, master, fortune, tabu, holey, whynot, poly]
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                botId:
                  type: string
                  enum: [random_bot, beginner_bot, medium_bot]
                  default: random_bot
                boardSize:
                  type: integer
                  default: 5
      responses:
        '201':
          description: Game created
        '400':
          description: Invalid game type or missing userId

  /api/game-manager/state/{id}:
    get:
      summary: Get game state
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Game state
        '401':
          description: Unauthorized
        '403':
          description: Forbidden
        '404':
          description: Game not found

  /api/game-manager/list:
    get:
      summary: List user games
      security:
        - bearerAuth: []
      responses:
        '200':
          description: List of games
        '401':
          description: Unauthorized

  /api/game-manager/game/{id}/move:
    post:
      summary: Make a move
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [coords]
              properties:
                coords:
                  type: object
                  properties:
                    x:
                      type: integer
                    y:
                      type: integer
      responses:
        '200':
          description: Move applied
        '400':
          description: Invalid move
        '401':
          description: Unauthorized
        '403':
          description: Forbidden
        '404':
          description: Game not found

  /api/game-manager/game/{id}/resign:
    post:
      summary: Resign a game
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Game resigned
        '400':
          description: Game already resigned
        '401':
          description: Unauthorized
        '403':
          description: Forbidden
        '404':
          description: Game not found